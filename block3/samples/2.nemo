VAR n: INT; VAR x: INT; VAR y: INT;
VAR damn_son : INT;
VAR huge_arr : [INT];
VAR huge1 : [[INT]];
VAR huge2 : [[[INT]]];
{
    n := 12; x := 0; y := 0;
    {
        {{
              (y <= n)?;
              y := (y + (x + (x + 1)));
              x := (x + 1);
        }}*;
        (NOT (y <= n))?;
    };
    x := (x - 1);
}
